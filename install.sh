#!/bin/bash
# Скрипт для быстрой установки и запуска бота локально

set -e

echo "🚀 Telegram Insights Bot - Установка и запуск"
echo "================================================"

# Проверка Python
if ! command -v python3 &> /dev/null
then
    echo "❌ Python3 не установлен. Установите Python 3.9 или выше."
    exit 1
fi

echo "✓ Python найден"
python3 --version

# Создание виртуального окружения
if [ ! -d "venv" ]; then
    echo "📦 Создание виртуального окружения..."
    python3 -m venv venv
    echo "✓ Виртуальное окружение создано"
else
    echo "✓ Виртуальное окружение уже существует"
fi

# Активация виртуального окружения
echo "🔧 Активация виртуального окружения..."
source venv/bin/activate

# Установка зависимостей
echo "📥 Установка зависимостей..."
pip install -q --upgrade pip
pip install -q -r requirements.txt
echo "✓ Зависимости установлены"

# Проверка .env файла
if [ ! -f ".env" ]; then
    echo "⚠️  Файл .env не найден"
    echo "📝 Создание .env из .env.example..."
    cp .env.example .env
    echo ""
    echo "⚠️  Отредактируйте файл .env с вашими данными:"
    echo "   - BOT_TOKEN - токен от @BotFather"
    echo "   - SUPABASE_URL - URL вашего Supabase проекта"
    echo "   - SUPABASE_KEY - API ключ Supabase"
    echo "   - WEBHOOK_URL - URL вашего приложения"
    echo ""
    read -p "Нажмите Enter после редактирования .env"
fi

echo "✓ Конфигурация готова"

# Запуск бота
echo ""
echo "🤖 Запуск бота..."
echo "================================================"
python main.py

# Деактивация окружения при выходе
trap "deactivate" EXIT
